apiVersion: v1
kind: ConfigMap
metadata:
  name: data-sync
  namespace: {{ namespace }}
  labels:
    mobile: enabled
    serviceName: data-sync
    serviceInstanceID: '{{_apb_service_instance_id}}'
data:
  id: data-sync
  type: data-sync
  name: data-sync
  uri: https://{{ data_sync_server_route.route.spec.host }}
